#include "main.h"
#include <string.h>
/**
 * _strlen - Returns the length of a string.
 * @s: Pointer to the input string.
 *
 * Return: The length of the string.
 */
int _strlen(char *s)
{
	return (strlen(s));

}
